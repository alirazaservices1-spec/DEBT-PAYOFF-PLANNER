<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DebtPath – Payoff Planner</title>
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&family=Nunito+Sans:wght@400;600;700;800&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    html, body {
      height: 100%;
      font-family: 'Nunito', sans-serif;
    }

    body {
      min-height: 100vh;
      background: #F5EDD6;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px 16px;
    }

    /* ── SCREEN CONTAINER ── */
    .screen {
      width: 100%;
      max-width: 390px;
      min-height: 100vh;
      background:
        radial-gradient(ellipse at 20% 60%, rgba(120,180,80,.13) 0%, transparent 50%),
        radial-gradient(ellipse at 80% 20%, rgba(245,200,80,.15) 0%, transparent 45%),
        #F5EDD6;
      border-radius: 0px;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 52px 32px 36px;
      position: relative;
      overflow: hidden;
    }

    /* ── TOP LABEL ── */
    .top-label {
      font-family: 'Nunito Sans', sans-serif;
      font-size: .68rem;
      font-weight: 800;
      color: #B8860B;
      letter-spacing: .18em;
      text-transform: uppercase;
      text-align: center;
      margin-bottom: 28px;
    }
    .top-label span {
      margin: 0 6px;
      opacity: .6;
    }

    /* ── MASCOT AREA ── */
    .mascot-wrap {
      position: relative;
      width: 220px;
      height: 220px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 12px;
    }

    /* shadow under mascot */
    .mascot-shadow {
      position: absolute;
      bottom: 8px;
      left: 50%;
      transform: translateX(-50%);
      width: 120px;
      height: 22px;
      background: rgba(180,130,60,.18);
      border-radius: 50%;
      filter: blur(6px);
    }

    /* sparkle dots */
    .spark {
      position: absolute;
      border-radius: 50%;
    }
    .spark-1 { width: 10px; height: 10px; background: #7DC95E; top: 38px; left: 14px; opacity: .7; }
    .spark-2 { width: 7px;  height: 7px;  background: #E8A020; top: 28px; right: 38px; opacity: .8; }
    .spark-3 { width: 8px;  height: 8px;  background: #E8A020; bottom: 50px; right: 20px; opacity: .6; }

    .dex-bob { animation: dexBob 2.8s ease-in-out infinite; }
    @keyframes dexBob {
      0%,100%{ transform: translateY(0) rotate(0deg); }
      45%{ transform: translateY(-8px) rotate(1.5deg); }
    }

    /* ── HEADLINE ── */
    .headline {
      text-align: center;
      margin-top: 12px;
      margin-bottom: 14px;
      line-height: 1.18;
    }
    .headline .black-text {
      font-family: 'Nunito', sans-serif;
      font-weight: 900;
      font-size: 2.25rem;
      color: #1a1a1a;
      display: block;
    }
    .headline .green-text {
      font-family: 'Nunito', sans-serif;
      font-weight: 900;
      font-size: 2.25rem;
      color: #2E7D32;
      display: block;
    }

    /* ── SUBTEXT ── */
    .subtext {
      font-family: 'Nunito Sans', sans-serif;
      font-size: 1rem;
      font-weight: 600;
      color: #5a5040;
      text-align: center;
      line-height: 1.55;
      margin-bottom: 36px;
      padding: 0 8px;
    }

    /* ── CTA BUTTON ── */
    .cta-btn {
      width: 100%;
      padding: 18px 24px;
      background: #2563EB;
      color: #fff;
      border: none;
      border-radius: 50px;
      font-family: 'Nunito', sans-serif;
      font-size: 1.15rem;
      font-weight: 800;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      letter-spacing: .01em;
      transition: transform .15s ease, background .15s ease;
      box-shadow: 0 8px 24px rgba(37,99,235,.35);
      margin-bottom: 20px;
    }
    .cta-btn:hover { background: #1d4ed8; transform: translateY(-2px); }
    .cta-btn:active { transform: scale(.98); }
    .cta-btn .arrow {
      font-size: 1.2rem;
      transition: transform .2s ease;
    }
    .cta-btn:hover .arrow { transform: translateX(4px); }

    /* ── FOOTER NOTE ── */
    .footer-note {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 5px;
    }
    .secure-line {
      font-family: 'Nunito Sans', sans-serif;
      font-size: .75rem;
      font-weight: 700;
      color: #7a6a50;
      display: flex;
      align-items: center;
      gap: 6px;
    }
    .secure-line svg {
      width: 13px; height: 13px;
      stroke: #7a6a50;
      fill: none;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
    }
    .already-link {
      font-family: 'Nunito Sans', sans-serif;
      font-size: .68rem;
      font-weight: 800;
      color: #B8860B;
      letter-spacing: .14em;
      text-transform: uppercase;
      text-decoration: none;
      cursor: pointer;
    }
    .already-link:hover { text-decoration: underline; }

    /* spacer to push button/footer down */
    .spacer { flex: 1; }
  </style>
</head>
<body>
<div class="screen">

  <!-- TOP LABEL -->
  <div class="top-label">DebtPath <span>·</span> Payoff Planner</div>

  <!-- MASCOT -->
  <div class="mascot-wrap">
    <div class="spark spark-1"></div>
    <div class="spark spark-2"></div>
    <div class="spark spark-3"></div>
    <div class="mascot-shadow"></div>
    <div class="dex-bob">
      <svg viewBox="0 0 140 140" width="200" height="200">
        <!-- shadow feet -->
        <ellipse cx="52" cy="128" rx="18" ry="7" fill="#9A6010" opacity=".35"/>
        <ellipse cx="90" cy="128" rx="18" ry="7" fill="#9A6010" opacity=".35"/>
        <!-- arms -->
        <ellipse cx="22" cy="72" rx="13" ry="22" fill="#C8880E" transform="rotate(20,22,72)"/>
        <ellipse cx="118" cy="68" rx="13" ry="22" fill="#C8880E" transform="rotate(-20,118,68)"/>
        <!-- waving hand raised -->
        <ellipse cx="118" cy="44" rx="11" ry="14" fill="#C8880E" transform="rotate(-30,118,44)"/>
        <!-- body -->
        <circle cx="70" cy="70" r="50" fill="#D4900A"/>
        <!-- belly shading -->
        <ellipse cx="70" cy="90" rx="34" ry="18" fill="#B87010" opacity=".2"/>
        <!-- ears -->
        <circle cx="30" cy="30" r="15" fill="#C07820"/>
        <circle cx="110" cy="30" r="15" fill="#C07820"/>
        <!-- eye whites -->
        <ellipse cx="52" cy="60" rx="20" ry="22" fill="white"/>
        <ellipse cx="88" cy="60" rx="20" ry="22" fill="white"/>
        <!-- pupils -->
        <circle cx="54" cy="63" r="14" fill="#1A0800"/>
        <circle cx="90" cy="63" r="14" fill="#1A0800"/>
        <!-- eye shine -->
        <circle cx="49" cy="56" r="5" fill="white"/>
        <circle cx="85" cy="56" r="5" fill="white"/>
        <!-- blush -->
        <ellipse cx="34" cy="80" rx="10" ry="6" fill="#E8823A" opacity=".28"/>
        <ellipse cx="106" cy="80" rx="10" ry="6" fill="#E8823A" opacity=".28"/>
        <!-- smile -->
        <path d="M46 88 Q70 112 94 88" stroke="#8A5010" stroke-width="5" fill="none" stroke-linecap="round"/>
        <!-- coin -->
        <circle cx="22" cy="88" r="15" fill="#F0C030" stroke="#C09020" stroke-width="2"/>
        <text x="16" y="94" style="font-size:14px;fill:#7A4A00;font-family:sans-serif;font-weight:900">$</text>
      </svg>
    </div>
  </div>

  <!-- HEADLINE -->
  <div class="headline">
    <span class="black-text">Make paying off debt</span>
    <span class="green-text">(and saving for<br>your dreams)</span>
    <span class="black-text">like a game.</span>
  </div>

  <!-- SUBTEXT -->
  <p class="subtext">
    Let's get started adding your<br>debts to pay off
  </p>

  <div class="spacer"></div>

  <!-- CTA BUTTON -->
  <button class="cta-btn">
    Get Started <span class="arrow">→</span>
  </button>

  <!-- FOOTER -->
  <div class="footer-note">
    <div class="secure-line">
      <svg viewBox="0 0 24 24">
        <rect x="3" y="11" width="18" height="11" rx="2"/>
        <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
      </svg>
      No account needed to start — secure
    </div>
    <a class="already-link">I already have the app</a>
  </div>

</div>
</body>
</html>