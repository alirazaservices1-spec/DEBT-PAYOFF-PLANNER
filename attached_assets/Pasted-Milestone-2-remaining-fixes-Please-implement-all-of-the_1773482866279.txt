Milestone 2 remaining fixes. Please implement all of the 
following and test each one before moving to the next:

---

## 1. XP PROGRESS BAR — VISUAL IMPROVEMENTS

- Make the XP progress bar thicker (minimum 12px height)
- Add a subtle green glow effect on the filled portion
- XP bar should feel rewarding and satisfying to look at
- "0 XP" label on the right should be styled consistently 
  with the rest of the card

---

## 2. COMBINE XP + STREAK INTO ONE ROW

On the dashboard, instead of XP and Streak as two separate 
tall cards stacked on top of each other:
- Put them side by side in one compact row
- Left card: XP level badge + progress bar
- Right card: Streak flame icon + day count
- This saves vertical space and makes the dashboard 
  less scrolly
- Both cards should be equal width

---

## 3. STREAK FLAME ICON — STATES

- When streak = 0: flame icon is grey/faded, 
  message shows "Start your streak today!"
- When streak > 0: flame icon is bold orange/red 
  with a subtle pulse animation
- When streak >= 7: flame icon is bright red, 
  slightly larger to show it's hot 🔥

---

## 4. DEX BADGE SIZE

- The green "DEX" badge on the mascot card is slightly 
  too large and competes with the speech bubble
- Reduce badge size by about 20%
- Keep same color and position

---

## 5. SMART STREAK REMINDER — PUSH NOTIFICATION

- Every day at 8:00 PM local time, check if the user 
  has logged a payment today
- If YES — do nothing
- If NO and streak > 0 — send push notification:
  "🔥 Don't break your streak! You haven't logged a 
  payment today. Tap to keep it alive."
- If NO and streak = 0 — send:
  "💪 Start a new streak today! Log a payment to begin."
- Tapping the notification opens the app directly 
  to the Log Payment screen
- User can turn this off from the More tab
- Default is ON
- Use Expo Notifications (already set up)

---

## 6. GRACE LOGIC

- If user misses one day and streak > 0, grace window 
  is active for 24 hours
- Only one grace day allowed per 7-day period
- Store grace day used status in AsyncStorage
- If grace already used and user misses another day, 
  streak resets to 0 immediately
- If grace not used and user logs payment within 
  24 hours of missing, streak is saved

---

## 7. NOTIFICATIONS — CONFLICT CHECK

Both of these notifications are now scheduled:
- 9:00 AM — Daily goal motivational reminder
- 8:00 PM — Streak reminder if no payment logged

Please confirm:
- Both can run simultaneously without conflict
- If user has no goal set, 9 AM notification 
  should not fire
- If streak is 0 and user has no goal, no 
  notifications should fire at all
- Test both on device by temporarily setting 
  times to 2-3 minutes from now, confirm both 
  fire correctly, then reset to 9 AM and 8 PM

---

## GENERAL

- All changes must work in dark mode
- All new state must persist in AsyncStorage
- Do not break any existing Milestone 1 or 
  Milestone 2 functionality
- Send QR code when all done so I can test 
  on device. ty!