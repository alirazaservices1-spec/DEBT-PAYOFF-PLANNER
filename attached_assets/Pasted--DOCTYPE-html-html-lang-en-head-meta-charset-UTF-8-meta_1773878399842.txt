<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Your Progress</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Playfair+Display:ital,wght@0,700;0,800;1,700&display=swap" rel="stylesheet"/>
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    background: #EDE8DC;
    font-family: 'DM Sans', 'Helvetica Neue', Arial, sans-serif;
    min-height: 100vh;
    display: flex;
    justify-content: center;
  }

  @keyframes fadeUp   { from { opacity:0; transform:translateY(16px); } to { opacity:1; transform:translateY(0); } }
  @keyframes popIn    { 0%{transform:scale(0.7);opacity:0} 70%{transform:scale(1.06)} 100%{transform:scale(1);opacity:1} }
  @keyframes dexFloat { 0%,100%{transform:translateY(0)} 50%{transform:translateY(-6px)} }
  @keyframes barFill  { from{width:0} to{width:var(--fill)} }
  @keyframes shimmer  { 0%{background-position:-200% center} 100%{background-position:200% center} }
  @keyframes glow     { 0%,100%{box-shadow:0 0 0 0 rgba(184,115,32,0.35)} 50%{box-shadow:0 0 0 10px rgba(184,115,32,0)} }
  @keyframes spin     { from{transform:rotate(0deg)} to{transform:rotate(360deg)} }

  .screen {
    width: 100%;
    max-width: 390px;
    background: #EDE8DC;
    position: relative;
    overflow: hidden;
    padding: 52px 24px 48px;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .blob-tr { position:absolute; top:-40px; right:-40px; width:170px; height:170px; border-radius:50%; background:#D4C9A8; opacity:.45; pointer-events:none; }
  .blob-bl { position:absolute; bottom:-50px; left:-50px; width:170px; height:170px; border-radius:50%; background:#D4C9A8; opacity:.4; pointer-events:none; }

  .page-label {
    font-size:11px; font-weight:600; letter-spacing:2.5px;
    color:#A07840; text-transform:uppercase;
    margin-bottom:20px; text-align:center;
    animation: fadeUp .4s ease .05s both;
  }

  /* ── Current level hero ── */
  .hero-card {
    width:100%;
    background: linear-gradient(145deg, #C8882A, #D9A045 50%, #B87320);
    background-size: 200% auto;
    animation: shimmer 4s linear infinite, popIn .55s cubic-bezier(.36,.07,.19,.97) .1s both;
    border-radius: 24px;
    padding: 22px 22px 20px;
    margin-bottom: 20px;
    box-shadow: 0 8px 28px rgba(180,120,30,.3);
    display: flex;
    align-items: center;
    gap: 18px;
    position: relative;
    overflow: hidden;
  }
  .hero-card::after {
    content:'';
    position:absolute; top:-30px; right:-30px;
    width:120px; height:120px;
    border-radius:50%;
    background:rgba(255,255,255,.08);
    pointer-events:none;
  }
  .level-badge-big {
    width:70px; height:70px;
    border-radius:20px;
    background:rgba(255,255,255,.18);
    border:2px solid rgba(255,255,255,.3);
    display:flex; flex-direction:column;
    align-items:center; justify-content:center;
    flex-shrink:0;
    animation: dexFloat 3s ease-in-out infinite;
  }
  .level-badge-big .lv-icon { font-size:28px; line-height:1; }
  .level-badge-big .lv-tag  { font-size:9px; font-weight:700; letter-spacing:1px; color:rgba(255,255,255,.8); text-transform:uppercase; margin-top:2px; }
  .hero-right { flex:1; }
  .hero-level-name {
    font-family:'Playfair Display',Georgia,serif;
    font-size:22px; font-weight:800;
    color:#fff; line-height:1.15; margin-bottom:3px;
  }
  .hero-xp-label { font-size:12px; color:rgba(255,240,200,.8); font-weight:500; margin-bottom:8px; }
  .progress-track {
    background:rgba(255,255,255,.2);
    border-radius:50px; height:10px; width:100%;
    overflow:hidden; position:relative;
  }
  .progress-fill {
    height:100%;
    background:#fff;
    border-radius:50px;
    --fill:58%;
    width:0;
    animation: barFill 1.2s cubic-bezier(.4,0,.2,1) .8s both;
    animation-fill-mode:forwards;
  }
  .progress-meta {
    display:flex; justify-content:space-between;
    margin-top:5px;
    font-size:11px; color:rgba(255,240,200,.75); font-weight:600;
  }

  /* ── Dream goal banner ── */
  .dream-banner {
    width:100%;
    background:#FFFFFF;
    border-radius:18px;
    padding:16px 18px;
    margin-bottom:20px;
    display:flex; align-items:center; gap:14px;
    box-shadow:0 2px 10px rgba(100,60,10,.07);
    animation: fadeUp .4s ease .55s both;
    border-left:4px solid #B87320;
  }
  .dream-icon-wrap {
    width:46px; height:46px;
    border-radius:14px;
    background:#F5F0E6;
    display:flex; align-items:center; justify-content:center;
    font-size:26px; flex-shrink:0;
    border:1.5px solid rgba(180,140,80,.2);
  }
  .dream-text-wrap { flex:1; }
  .dream-eyebrow { font-size:10px; font-weight:700; letter-spacing:1.5px; text-transform:uppercase; color:#A07840; margin-bottom:2px; }
  .dream-title   { font-size:14px; font-weight:700; color:#1C0F00; line-height:1.3; margin-bottom:1px; }
  .dream-sub     { font-size:12px; color:#8B6840; line-height:1.4; }

  /* ── Section label ── */
  .section-label {
    width:100%; font-size:11px; font-weight:700;
    letter-spacing:2px; text-transform:uppercase;
    color:#A07840; margin-bottom:12px;
    animation: fadeUp .4s ease .65s both;
  }

  /* ── Levels list ── */
  .levels-list {
    width:100%;
    display:flex; flex-direction:column; gap:10px;
    margin-bottom:24px;
  }

  .level-row {
    background:#FFFFFF;
    border-radius:16px;
    padding:13px 16px;
    display:flex; align-items:center; gap:14px;
    box-shadow:0 1px 6px rgba(100,60,10,.06);
    position:relative;
    opacity:0;
    animation: fadeUp .35s ease both;
    transition: transform .15s;
  }
  .level-row:hover { transform:translateX(3px); }

  .level-row.current {
    background:#fff;
    border:2px solid #D9A045;
    box-shadow:0 4px 16px rgba(180,120,30,.15);
    animation: fadeUp .35s ease both, glow 2.5s ease-in-out infinite;
  }
  .level-row.locked {
    opacity:.55 !important;
  }
  .level-row.locked .lv-icon-wrap { filter:grayscale(1); }

  .lv-icon-wrap {
    width:46px; height:46px;
    border-radius:14px;
    background:#F5F0E6;
    display:flex; align-items:center; justify-content:center;
    font-size:24px; flex-shrink:0;
    border:1.5px solid rgba(180,140,80,.15);
  }
  .level-row.current .lv-icon-wrap {
    background: linear-gradient(135deg,#C8882A,#D9A045);
    border:none;
  }

  .lv-info { flex:1; }
  .lv-name {
    font-size:14px; font-weight:700;
    color:#1C0F00; margin-bottom:1px; line-height:1.2;
  }
  .lv-xp-range {
    font-size:12px; color:#8B6840; font-weight:500;
  }
  .lv-right { text-align:right; flex-shrink:0; }
  .lv-status-done {
    font-size:18px;
  }
  .lv-status-current {
    background:#D9A045;
    color:#fff;
    font-size:10px; font-weight:700;
    letter-spacing:1px; text-transform:uppercase;
    padding:4px 10px; border-radius:50px;
  }
  .lv-status-locked {
    font-size:18px; opacity:.5;
  }

  .current-tag {
    position:absolute; top:-9px; left:16px;
    background:#3D2200; color:#F5EFE0;
    font-size:9px; font-weight:700; letter-spacing:1.5px;
    text-transform:uppercase; padding:3px 10px; border-radius:50px;
  }

  /* ── Motivational nudge ── */
  .nudge-card {
    width:100%;
    background:#3D2200;
    border-radius:20px;
    padding:20px 20px;
    margin-bottom:24px;
    display:flex; gap:14px; align-items:flex-start;
    animation: fadeUp .4s ease 1.05s both;
  }
  .nudge-dex {
    animation: dexFloat 3.5s ease-in-out infinite;
    flex-shrink:0; margin-top:2px;
  }
  .nudge-text { flex:1; }
  .nudge-head {
    font-family:'Playfair Display',Georgia,serif;
    font-size:16px; font-weight:800;
    color:#F5EFE0; margin-bottom:5px; line-height:1.3;
  }
  .nudge-head em { color:#D9A045; font-style:italic; }
  .nudge-body {
    font-size:13px; color:#F5EFE0;
    line-height:1.55; font-weight:500;
  }

  /* ── CTA ── */
  .cta {
    width:100%;
    background:#3D2200; color:#F5EFE0;
    border:none; border-radius:50px;
    padding:18px 0; font-size:17px;
    font-family:'DM Sans',sans-serif; font-weight:600;
    cursor:pointer; letter-spacing:.2px;
    transition:background .2s, transform .15s;
    animation: fadeUp .4s ease 1.15s both;
  }
  .cta:hover { background:#2a1600; transform:scale(1.01); }
  .cta:active { transform:scale(.99); }
</style>
</head>
<body>
<div class="screen">
  <div class="blob-tr"></div>
  <div class="blob-bl"></div>

  <p class="page-label">YOUR PROGRESS</p>

  <!-- ── Dex mascot ── -->
  <div style="animation: dexFloat 3s ease-in-out infinite; margin-bottom:14px;">
    <svg width="90" height="94" viewBox="0 0 110 115" fill="none" xmlns="http://www.w3.org/2000/svg">
      <circle cx="22" cy="32" r="17" fill="#C8882A"/>
      <circle cx="88" cy="32" r="17" fill="#C8882A"/>
      <circle cx="22" cy="32" r="10" fill="#D9A045" opacity=".7"/>
      <circle cx="88" cy="32" r="10" fill="#D9A045" opacity=".7"/>
      <circle cx="55" cy="57" r="36" fill="#D9A045"/>
      <ellipse cx="43" cy="42" rx="10" ry="7" fill="white" opacity=".18" transform="rotate(-25 43 42)"/>
      <ellipse cx="55" cy="71" rx="16" ry="11" fill="#C8882A" opacity=".5"/>
      <!-- excited wide eyes -->
      <circle cx="41" cy="52" r="10" fill="#1C0F00"/>
      <circle cx="69" cy="52" r="10" fill="#1C0F00"/>
      <circle cx="44" cy="48" r="4" fill="white"/>
      <circle cx="72" cy="48" r="4" fill="white"/>
      <circle cx="46" cy="50" r="1.5" fill="white" opacity=".6"/>
      <circle cx="74" cy="50" r="1.5" fill="white" opacity=".6"/>
      <ellipse cx="55" cy="67" rx="4.5" ry="3" fill="#8B5E20" opacity=".75"/>
      <path d="M42,73 Q55,83 68,73" stroke="#8B5E20" stroke-width="3" stroke-linecap="round" fill="none" opacity=".85"/>
      <ellipse cx="30" cy="63" rx="8" ry="5" fill="#E8955A" opacity=".35"/>
      <ellipse cx="80" cy="63" rx="8" ry="5" fill="#E8955A" opacity=".35"/>
      <text x="5"  y="26" font-size="13">✨</text>
      <text x="88" y="22" font-size="11">⭐</text>
      <ellipse cx="55" cy="103" rx="22" ry="14" fill="#C8882A"/>
      <circle cx="55" cy="100" r="7" fill="#D9A045" opacity=".5"/>
      <text x="55" y="103.5" text-anchor="middle" font-size="8" fill="#8B5E20" font-weight="bold" opacity=".9">$</text>
    </svg>
  </div>
  <div class="hero-card">
    <div class="level-badge-big">
      <span class="lv-icon">🪙</span>
      <span class="lv-tag">Lv 4</span>
    </div>
    <div class="hero-right">
      <p class="hero-level-name">Gold Saver</p>
      <p class="hero-xp-label">1,240 XP · 260 to next level</p>
      <div class="progress-track">
        <div class="progress-fill" style="--fill:58%"></div>
      </div>
      <div class="progress-meta">
        <span>1,000 XP</span>
        <span>58%</span>
        <span>1,500 XP</span>
      </div>
    </div>
  </div>

  <!-- ── Dream goal banner ── -->
  <div class="dream-banner">
    <div class="dream-icon-wrap">🏖️</div>
    <div class="dream-text-wrap">
      <p class="dream-eyebrow">Your Dream Goal</p>
      <p class="dream-title">Beach House Vacation</p>
      <p class="dream-sub">Stay consistent - you're on track to unlock it!</p>
    </div>
  </div>

  <!-- ── All levels ── -->
  <p class="section-label">All Levels</p>

  <div class="levels-list">

    <!-- Done -->
    <div class="level-row" style="animation-delay:.7s">
      <div class="lv-icon-wrap">🌱</div>
      <div class="lv-info">
        <p class="lv-name">Seedling</p>
        <p class="lv-xp-range">0 – 99 XP</p>
      </div>
      <div class="lv-right"><span class="lv-status-done">✅</span></div>
    </div>

    <div class="level-row" style="animation-delay:.77s">
      <div class="lv-icon-wrap">🥉</div>
      <div class="lv-info">
        <p class="lv-name">Bronze Starter</p>
        <p class="lv-xp-range">100 – 299 XP</p>
      </div>
      <div class="lv-right"><span class="lv-status-done">✅</span></div>
    </div>

    <div class="level-row" style="animation-delay:.84s">
      <div class="lv-icon-wrap">⚡</div>
      <div class="lv-info">
        <p class="lv-name">Momentum Builder</p>
        <p class="lv-xp-range">300 – 999 XP</p>
      </div>
      <div class="lv-right"><span class="lv-status-done">✅</span></div>
    </div>

    <!-- Current -->
    <div class="level-row current" style="animation-delay:.91s">
      <div class="current-tag">YOU ARE HERE</div>
      <div class="lv-icon-wrap">🪙</div>
      <div class="lv-info">
        <p class="lv-name">Gold Saver</p>
        <p class="lv-xp-range">1,000 – 1,499 XP</p>
      </div>
      <div class="lv-right"><span class="lv-status-current">IN PROGRESS</span></div>
    </div>

    <!-- Locked -->
    <div class="level-row locked" style="animation-delay:.98s">
      <div class="lv-icon-wrap">💎</div>
      <div class="lv-info">
        <p class="lv-name">Diamond Warrior</p>
        <p class="lv-xp-range">1,500 – 2,499 XP</p>
      </div>
      <div class="lv-right"><span class="lv-status-locked">🔒</span></div>
    </div>

    <div class="level-row locked" style="animation-delay:1.05s">
      <div class="lv-icon-wrap">🏆</div>
      <div class="lv-info">
        <p class="lv-name">Freedom Champion</p>
        <p class="lv-xp-range">2,500 – 4,999 XP</p>
      </div>
      <div class="lv-right"><span class="lv-status-locked">🔒</span></div>
    </div>

    <div class="level-row locked" style="animation-delay:1.12s">
      <div class="lv-icon-wrap">🌟</div>
      <div class="lv-info">
        <p class="lv-name">Debt-Free Legend</p>
        <p class="lv-xp-range">5,000+ XP</p>
      </div>
      <div class="lv-right"><span class="lv-status-locked">🔒</span></div>
    </div>

  </div>

  <!-- ── Dex motivational nudge ── -->
  <div class="nudge-card">
    <div class="nudge-dex">
      <svg width="52" height="55" viewBox="0 0 110 115" fill="none" xmlns="http://www.w3.org/2000/svg">
        <circle cx="22" cy="32" r="17" fill="#C8882A"/>
        <circle cx="88" cy="32" r="17" fill="#C8882A"/>
        <circle cx="22" cy="32" r="10" fill="#D9A045" opacity=".7"/>
        <circle cx="88" cy="32" r="10" fill="#D9A045" opacity=".7"/>
        <circle cx="55" cy="57" r="36" fill="#D9A045"/>
        <ellipse cx="43" cy="42" rx="10" ry="7" fill="white" opacity=".18" transform="rotate(-25 43 42)"/>
        <ellipse cx="55" cy="71" rx="16" ry="11" fill="#C8882A" opacity=".5"/>
        <circle cx="41" cy="53" r="9" fill="#1C0F00"/>
        <circle cx="69" cy="53" r="9" fill="#1C0F00"/>
        <circle cx="44" cy="50" r="3.2" fill="white"/>
        <circle cx="72" cy="50" r="3.2" fill="white"/>
        <ellipse cx="55" cy="67" rx="4.5" ry="3" fill="#8B5E20" opacity=".8"/>
        <path d="M44,74 Q55,82 66,74" stroke="#8B5E20" stroke-width="2.5" stroke-linecap="round" fill="none" opacity=".85"/>
        <ellipse cx="32" cy="64" rx="7" ry="4.5" fill="#E8955A" opacity=".3"/>
        <ellipse cx="78" cy="64" rx="7" ry="4.5" fill="#E8955A" opacity=".3"/>
        <ellipse cx="55" cy="103" rx="22" ry="14" fill="#C8882A"/>
        <circle cx="55" cy="100" r="7" fill="#D9A045" opacity=".5"/>
        <text x="55" y="103.5" text-anchor="middle" font-size="8" fill="#8B5E20" font-weight="bold" opacity=".9">$</text>
      </svg>
    </div>
    <div class="nudge-text">
      <p class="nudge-head">Just <em>260 more XP</em> to Diamond Warrior!</p>
      <p class="nudge-body">Log a payment every day this week and you'll unlock it before the weekend. Your Beach House dream is counting on you! 🏖️</p>
    </div>
  </div>

  <button class="cta">🔥 Keep My Streak Going</button>

</div>
</body>
</html>